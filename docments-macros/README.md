# docments-macros

The `#[docments]` attribute behind the [docments](https://crates.io/crates/docments) crate. Depend on `docments`, which re-exports it together with the runtime registry and renderers; this crate has no API of its own.
